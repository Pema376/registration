<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home | Sherubtse College</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', sans-serif;
            background: url("/images/clocktower.jpg") no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            color: #fff;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .overlay {
            background-color: rgba(0, 0, 0, 0.6);
            padding: 40px;
            margin: 50px auto;
            width: 80%;
            max-width: 900px;
            border-radius: 10px;
        }

        h1 {
            text-align: center;
            font-size: 2.5rem;
            color: #fcd307;
            margin-bottom: 20px;
        }

        h2 {
            color: #fcd307;
            margin-top: 20px;
        }

        p {
            font-size: 1.1rem;
            line-height: 1.6;
        }

        .logout-container {
            display: flex;
            justify-content: flex-end;
            padding: 20px;
        }

        .logout-btn {
            padding: 10px 20px;
            background-color: #fcd307;
            color: #002147;
            border: none;
            border-radius: 5px;
            font-weight: bold;
            cursor: pointer;
            font-size: 1rem;
            transition: background-color 0.3s ease;
        }

        .logout-btn:hover {
            background-color: #ffe600;
        }

        @media (max-width: 600px) {
            .overlay {
                padding: 20px;
            }

            h1 {
                font-size: 2rem;
            }

            p {
                font-size: 1rem;
            }
        }
    </style>
</head>

<body>

    <div class="logout-container">
        <button class="logout-btn" onclick="logout()">Logout</button>
    </div>

    <div class="overlay">
        <h1>Welcome to Sherubtse College</h1>
        <h2>Mission</h2>
        <p>To provide high-quality education that promotes academic excellence, personal growth, and professional
            development, preparing students to serve the nation with integrity and wisdom.</p>

        <h2>Vision</h2>
        <p>To be a premier center of learning that fosters innovation, leadership, and social responsibility in the
            pursuit of national development and global understanding.</p>
    </div>

    <script>
        function logout() {
            const confirmed = confirm("Are you sure you want to logout?");
            if (confirmed) {
                window.location.href = "/";
            }
        }
    </script>
</body>

</html>